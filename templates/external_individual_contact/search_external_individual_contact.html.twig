{% extends 'base.html.twig' %}

{% block title %}Contacts externes personnes physiques{% endblock %}

{% block body %}

{# {{ include ("partials/external_individual_contact.html.twig") }} #}
{{ include("partials/title_section.html.twig", {'name':'seach'}) }}

<div class="section">
	<div class="container">
    	<div class="row">
			<div class="col-lg-9">
                <div class="row shop_container">
                    {% for externalIndividualContact in externalIndividualContacts %}
                        {{ include ("partials/external_individual_contact.html.twig") }}
                    {% endfor %}
                </div>
        	</div>
            <div class="col-lg-3 order-lg-first mt-4 pt-2 mt-lg-0 pt-lg-0">
                    <div class="sidebar">
                        {{ form_start(search) }}
                        <div class="widget">
                            <h5 class="widget_title">Categories</h5>
                            
                        </div>
                        <div class="widget">
                            <h5 class="widget_title">Price</h5>

                        </div>
                        <div class="widget">
                            <h5 class="widget_title">Tags</h5>

                        </div>
                        <button class="btn btn-border-fill btn-block"> Search</button>
                        {{ form_end(search) }}
                    </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}